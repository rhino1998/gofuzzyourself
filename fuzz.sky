def random_int(x):
    def y():
        return x*4
    return y

def open_file(name,mode):
    def y():
        return [open(name, mode), "hello"]
    return y

tests = ["cat", "cat"]
runs = 5
format = "%d \t%s\n"
error_format = "%d %dX\t%s\n"

stdin = ["hello", ("w" , [a for a in "orld".split_bytes()]), test.run+5000]
stdout = [os.stdout]
stderr = os.stderr
args=[]
vars=dict()
